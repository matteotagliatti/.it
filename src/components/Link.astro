---
export interface Props {
  href: string;
  blank?: boolean;
}

const { href, blank = true } = Astro.props;
---

<a
  href={href}
  class="text-white font-medium underline hover:cursor-pointer"
  target={blank ? '_blank' : null}><slot /></a
>
